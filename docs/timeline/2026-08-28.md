# Learning Timeline — 2026-08-28

- [Codeforces 2249B](../../src/codeforces/set2/set22/set224/set2249/b/): Prefix maxima left of `n` and suffix maxima right of it force compressed `a` to rise then fall with unique block values; the first occurrence of each `x` is forced, later ones multiply by `x-used` unused smaller choices, then times 2 for swapping `n` with the first `n-1`.
- [Codeforces 2257D](../../src/codeforces/set2/set22/set225/set2257/d/): Area-`S` origin rectangles form a descending divisor staircase; prefix segment areas plus binary search on the first `w > x` and last `h > y` clip each query in `O(log d)` after `O(√S)` enumeration.
