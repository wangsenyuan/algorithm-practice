# Learning Timeline — 2026-08-12

- [Codeforces 2223B](../../src/codeforces/set2/set22/set222/set2223/b/): Expected inversions after a random module permutation equal the sum over `i<j` of `Pr[a_i x > a_j y]`; sort reduced ratios `a_i/a_j` and ordered `y/x`, two-pointer-count, then multiply by `(n(n-1))^{-1}` mod `998244353`.
- [Codeforces 2244F](../../src/codeforces/set2/set22/set224/set2244/f/): Cyclic child shifts keep each subtree a contiguous leaf segment, so bottom-up rotate child value-intervals to start at the subtree minimum, merge abutting ranges, and accept only when every node collapses to a single interval.
- [Codeforces 2245B](../../src/codeforces/set2/set22/set224/set2245/b/): Sort `a`, then two-pointer: each `a[l] < c` pairs with the current largest `a[r]` contributing `a[r]-c`, and every leftover element contributes `a[i]-c`.
- [Codeforces 2246C](../../src/codeforces/set2/set22/set224/set2246/c/): Even picks from every equal-value group give `2^(n-d)` zero alternating sums; when `-1` is present, also count odd `-1` picks that flip exactly one consecutive positive pair `(v,v+1)` to odd (still `2^(s-1)` choices per group).
