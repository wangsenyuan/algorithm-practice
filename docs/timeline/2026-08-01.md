# Learning Timeline — 2026-08-01

- [Codeforces 2226C](../../src/codeforces/set2/set22/set222/set2226/c/): A value `x` can become residue `r` iff `x = r` or `x > 2r`; after keeping one copy of each present `v < w`, binary-search the largest MEX by greedily assigning the smallest spare above `2r` to each missing `r`.
- [Codeforces 2226E](../../src/codeforces/set2/set22/set222/set2226/e/): Prefix answers stay monotone, so a lazy segment tree tracks Hall balances `#offerers with x ≥ 2q+1` minus `#receivers ≥ q`; each insertion range-adds and a while-loop lifts `mex` whenever the prefix minimum is nonnegative.
- [Codeforces 2227F](../../src/codeforces/set2/set22/set222/set2227/f/): With rightward gravity, each height packs its live cubes into the rightmost slots, so total distance is destination-sum minus a Fenwick of original indices; the best single top-cube removal then adds the maximum count of leftward columns of height at least that column's.
