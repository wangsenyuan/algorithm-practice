You are given an array 𝑎1,𝑎2,…,𝑎𝑛
 of positive integers.

You can color some elements of the array red, but there cannot be two adjacent red elements (i.e., for 1≤𝑖≤𝑛−1
, at least one of 𝑎𝑖
 and 𝑎𝑖+1
 must not be red).

Your score is the maximum value of a red element, plus the minimum value of a red element, plus the number of red elements. Find the maximum score you can get.

### ideas
1. max(red) + min(red) + count(red)
2. 假设目前选择了w个数字， 其中最大值为x，最小值为y, x + y + w
3. 考察新的数字z, 如果 z >= y => x + y + w + 1 (至少是)
4. 如果 z < y (比如 z = y - 1); x + z + w + 1 = x + y + w (没有更好)
5. 如果 z = y - 2, 那么就是一个更差的结果
6. 除非能选择两个z（可以抵消掉）
7. 假设a是排好序的。能处理吗？
8. 那么结果肯定是一个后缀，a[n], a[n-2], a[n-4](每一个的最大值)
9. 那么现在假设选择的最小值是a[i](其他选择的不能比它小)
10. 那么肯定是选择最大值（如果和i不靠近的情况下）
11. 从上面的分析来看，最大值肯定要被选中的。假设a[i]是最大值，a[i-1] < a[i], a[i+1] < a[i]
12. 假设a[i]没有被选中，而a[i-1], a[i+1] 被选中了，但是选中a[i]后，不选中i-1, i+1, 结果不会更差
13. 所以，肯定是最大值被选中了
14. 如果是 a[l...r]都是最大值，那么如果是奇数长度，那么就是这段的一半多一个
15. 如果是偶数，似乎有点麻烦，比如a[i], a[i+1]都是最大值
16. 但是貌似可以划分成两个子问题，[0...i-1]的最大值 + a[i+1], 或者 [i+1...n]的最大值 + a[i]
17. 如果是不连续的呢？