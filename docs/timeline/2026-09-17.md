# Learning Timeline — 2026-09-17

- [Luogu P3157](../../src/luogu/set3/p3157/): Start from the full inversion count; CDQ on value-sorted points then Fenwick-on-deletion-time charges each remaining pair to the earlier-deleted endpoint, so each deletion subtracts exactly the inversions that die with it.
- [Luogu P3810](../../src/luogu/set3/p3810/): Collapse identical triples and seed each group with `freq-1`; CDQ on `a` then two-pointers on `b` with a Fenwick on `c`, keeping the running answer on the item so the post-merge `(b,c)` sort stays aligned.
- [Luogu P4390](../../src/luogu/set4/p4390/): Added a documented TODO scaffold for point-add / rectangle-sum queries on a `W x W` grid; `drive` reads the command stream, and the official sample remains a skipped regression target.
