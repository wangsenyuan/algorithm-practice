# Learning Timeline — 2026-09-04

- [Codeforces 659G](../../src/codeforces/set0/set6/set650/set659/g/): A left-to-right DP adds single-board top cuts `h_i-1` and extensions through the bottleneck `min(h_{i-1},h_i)-1`; the live prefix that can still grow is scaled by `min(h_{i-1},h_i,h_{i+1})-1` before adjoining the next board, all mod `10^9+7`.
- [Codeforces 2038C](../../src/codeforces/set2/set20/set203/set2038/c/): Pair equal values into tokens; four tokens are required, and max area is the better product of spans from the two smallest as lows and the two largest as highs.
- [Codeforces 2038A](../../src/codeforces/set2/set20/set203/set2038/a/): Each engineer caps at `⌊a_i/b_i⌋`; if the total cap is `< k` everyone does 0, otherwise a left-to-right scan does only `max(0, k-suf[i+1]-tot)`, free-riding on later maximums.
